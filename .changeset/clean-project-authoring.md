---
"@voyant-travel/cli": minor
---

Add graph-native project authoring: expand the `operator-standard` preset into a clean explicit project, scaffold selectable local modules with optional facets, and install/select package or path dependencies with idempotent `add` and `install` commands.
